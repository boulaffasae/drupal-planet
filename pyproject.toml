[project]
name = "drupal-planet"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4",
    "lxml",
    "mistralai",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]
